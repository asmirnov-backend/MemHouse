type Mem {
  imgUrls: [String!]!
  text: String
  likes: Int!
  dislikes: Int!
  rating: Float!
}

type Query {
  mems(offset: Int!, limit: Int!): [Mem!]!
}

type Mutation {
  createMem(createMemInput: CreateMemInput): Mem
}

input CreateMemInput {
  imgUrls: [String!]!
  text: String
}
